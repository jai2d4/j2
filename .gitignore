__pycache__/
*.pyc
.env
.venv/
venv/
.pytest_cache/

# Out-of-tree CMake build directories at the repository root. trace/.gitignore
# only covers paths under trace/, so a build configured here was not ignored.
/build/
/build-*/
